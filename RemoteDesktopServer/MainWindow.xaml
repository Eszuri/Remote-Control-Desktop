<Window x:Class="RemoteDesktopServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PC Remote Control Server" Height="680" Width="820"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A" Foreground="#F8FAFC"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E293B" CornerRadius="12" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="🚀 PC Remote Control Server" FontSize="20" FontWeight="Bold" Foreground="#38BDF8"/>
                    <TextBlock Text="Native Windows Remote Server for Android Client" FontSize="13" Foreground="#94A3B8" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border x:Name="StatusBadge" Background="#EF4444" CornerRadius="12" Padding="10,4" Margin="0,0,12,0">
                        <TextBlock x:Name="TxtStatus" Text="STOPPED" FontSize="12" FontWeight="Bold" Foreground="White"/>
                    </Border>
                    <Button x:Name="BtnToggleServer" Content="Start Server" Width="130" Click="BtnToggleServer_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Connection Info & QR -->
            <Border Grid.Column="0" Background="#1E293B" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="📱 Connect from Mobile" FontSize="16" FontWeight="Bold" Foreground="#F1F5F9" Margin="0,0,0,12"/>

                        <!-- QR Code Container -->
                        <Border Background="#0F172A" CornerRadius="8" Padding="10" HorizontalAlignment="Center" Margin="0,0,0,14">
                            <Image x:Name="ImgQrCode" Width="180" Height="180" Stretch="Uniform"/>
                        </Border>

                        <!-- Info items -->
                        <TextBlock Text="Local IP Address:" Foreground="#94A3B8" FontSize="12"/>
                        <TextBox x:Name="TxtIpAddress" IsReadOnly="True" Margin="0,4,0,10" FontWeight="Bold"/>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                <TextBlock Text="Port:" Foreground="#94A3B8" FontSize="12"/>
                                <TextBox x:Name="TxtPort" Text="9090" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                <TextBlock Text="PIN Code:" Foreground="#94A3B8" FontSize="12"/>
                                <TextBox x:Name="TxtPin" Text="123456" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Connected Clients:" Foreground="#94A3B8" FontSize="12"/>
                        <TextBlock x:Name="TxtClientCount" Text="0 Devices" FontSize="16" FontWeight="Bold" Foreground="#10B981" Margin="0,4,0,14"/>

                        <Separator Background="#334155" Margin="0,0,0,14"/>

                        <!-- Stream settings -->
                        <TextBlock Text="⚙️ Stream Settings" FontSize="14" FontWeight="Bold" Foreground="#F1F5F9" Margin="0,0,0,8"/>

                        <TextBlock Text="Target FPS:" Foreground="#94A3B8" FontSize="12"/>
                        <DockPanel Margin="0,4,0,10">
                            <TextBlock x:Name="TxtFpsVal" Text="30 FPS" Foreground="#38BDF8" Width="50" DockPanel.Dock="Right" TextAlignment="Right"/>
                            <Slider x:Name="SliderFps" Minimum="10" Maximum="60" Value="30" TickFrequency="5" IsSnapToTickEnabled="True" ValueChanged="SliderFps_ValueChanged"/>
                        </DockPanel>

                        <TextBlock Text="JPEG Quality:" Foreground="#94A3B8" FontSize="12"/>
                        <DockPanel Margin="0,4,0,10">
                            <TextBlock x:Name="TxtQualityVal" Text="70%" Foreground="#38BDF8" Width="50" DockPanel.Dock="Right" TextAlignment="Right"/>
                            <Slider x:Name="SliderQuality" Minimum="20" Maximum="95" Value="70" TickFrequency="5" IsSnapToTickEnabled="True" ValueChanged="SliderQuality_ValueChanged"/>
                        </DockPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Panel: Live Activity Logs -->
            <Border Grid.Column="1" Background="#1E293B" CornerRadius="12" Padding="16" Margin="4,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="📋 Server Activity Logs" FontSize="16" FontWeight="Bold" Foreground="#F1F5F9"/>
                        <Button Content="Clear" HorizontalAlignment="Right" Padding="8,4" FontSize="12" Click="BtnClearLogs_Click"/>
                    </DockPanel>

                    <TextBox x:Name="TxtLogs" Grid.Row="1"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="#0B1329"
                             Foreground="#38BDF8"
                             BorderThickness="0"/>

                    <TextBlock Grid.Row="2" Text="Tip: Scan the QR code using Android app or connect to ws://IP:PORT"
                               Foreground="#64748B" FontSize="12" Margin="0,8,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock Text="Status: Ready" x:Name="TxtFooterStatus" Foreground="#94A3B8" FontSize="12"/>
                <TextBlock Text="Remote Desktop Windows Server v1.0" HorizontalAlignment="Right" Foreground="#64748B" FontSize="12"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
